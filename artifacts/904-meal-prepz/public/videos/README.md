# Video assets

- `kitchen-prep.mp4` — supplied portrait kitchen/meal-prep footage used in the kitchen story section.
- `meal-lineup.mp4` — supplied portrait meal footage used in the editorial gallery.

The source `.mov` files were converted to browser-friendly H.264 MP4 files with fast-start metadata. Replace these files with higher-resolution originals later if desired, keeping the same filenames or updating the `<source>` tags in `src/App.tsx`.